document.addEventListener(
  "DOMContentLoaded",
  () => {
    console.log("pictureApp JS imported successfully!");
  },
  false
);

// Template.registerHelper('reverseArray', (array) => array.reverse());

// Handlebars.registerHelper('reverse', function (arr) {
//   arr.reverse();
// });
